---
resources:
- name: pal-tracker-distributed
  type: git
  source:
    uri: {{github-repository}}
    branch: my-work
    private_key: {{github-private-key}}

- name: version
  type: semver
  source:
    bucket: {{aws-bucket}}
    key: pal-tracker-distributed/current-version
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: allocations-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: pal-tracker-distributed/allocations-server-(.*).jar
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: backlog-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: pal-tracker-distributed/backlog-server-(.*).jar
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: registration-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: pal-tracker-distributed/registration-server-(.*).jar
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: timesheets-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: pal-tracker-distributed/timesheets-server-(.*).jar
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: cf
  type: cf
  source:
    api: {{cf-api-url}}
    username: {{cf-username}}
    password: {{cf-password}}
    organization: {{cf-org}}
    space: sandbox
    skip_cert_check: true


jobs:
- name: build
  plan:
  - get: pal-tracker-distributed
    trigger: true
  - get: version
    params:
      bump: patch
  - task: build and test
    file: pal-tracker-distributed/ci/build.yml
  - put: allocations-artifacts
    params:
      file: build-output/allocations*.jar
  - put: backlog-artifacts
    params:
      file: build-output/backlog*.jar
  - put: registration-artifacts
    params:
      file: build-output/registration*.jar
  - put: timesheets-artifacts
    params:
      file: build-output/timesheets*.jar
  - put: version
    params:
      file: version/number

- name: deploy-allocations
  plan:
  - get: pal-tracker-distributed
    passed: [build]
    trigger: true
  - get: allocations-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker-distributed/manifest-allocations.yml
      path: allocations-artifacts/allocations-server*.jar
      current_app_name: tracker-allocations

- name: deploy-backlog
  plan:
  - get: pal-tracker-distributed
    passed: [build]
    trigger: true
  - get: backlog-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker-distributed/manifest-backlog.yml
      path: backlog-artifacts/backlog-server*.jar
      current_app_name: tracker-backlog

- name: deploy-registration
  plan:
  - get: pal-tracker-distributed
    passed: [build]
    trigger: true
  - get: registration-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker-distributed/manifest-registration.yml
      path: registration-artifacts/registration-server*.jar
      current_app_name: tracker-registration

- name: deploy-timesheets
  plan:
  - get: pal-tracker-distributed
    passed: [build]
    trigger: true
  - get: timesheets-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker-distributed/manifest-timesheets.yml
      path: timesheets-artifacts/timesheets-server*.jar
      current_app_name: tracker-timesheets
